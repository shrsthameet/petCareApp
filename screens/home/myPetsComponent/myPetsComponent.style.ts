import { StyleSheet } from 'react-native';

export const myPetsStyles = StyleSheet.create({
  petsContainer: {
    marginTop: 30,
  },
  petsContent: {
    paddingTop: 10,
    paddingBottom: 20,
    paddingHorizontal: 25,
  },
});